exec('import sys, math, re') or ((globals().__setitem__('table', list(range(1, int(sys.argv[1]) + 1))) or not list(map(lambda i: table[i - 1] is not None and list(map(lambda j: table.__setitem__(j - 1, None), range(i + i, int(sys.argv[1]) + 1, i))), range(2, math.ceil(math.sqrt(int(sys.argv[1]))) + 1))) or globals().__setitem__('_i', iter(((print(o, end=', ') if o != 1 and o else None) or _i for o in table))) or exec(('while True:\n'+' '*4+'try:\n'+' '*8+'next(%s)\n'+' '*4+'except StopIteration:\n'+' '*8+'break') % '_i') or print('...')) if len(sys.argv) > 1 and re.match(r'^\d+$', sys.argv[1]) and int(sys.argv[1]) > 0 else print('Usage: python3 ' + sys.argv[0] + ' NUMBER\nNUMBER must be a positive integer more than 0.'))